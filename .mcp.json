{
  "mcpServers": {
    "analyzer": {
      "command": "sh",
      "args": [
        "-c",
        "PATH=\"$PWD/node_modules/.bin:$PATH\" uv run mcp-server-analyzer"
      ],
      "env": {
        "PYTHONPATH": "src"
      }
    }
  }
}
